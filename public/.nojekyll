# Tells GitHub Pages to serve these files as-is instead of running them through Jekyll.
# Without it, Pages ignores any directory starting with an underscore — which is where
# Astro puts its hashed CSS and JS assets. The site would deploy with no styling at all.
